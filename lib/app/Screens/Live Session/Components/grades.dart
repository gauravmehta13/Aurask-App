import 'package:flutter/material.dart';

class SessionGrades extends StatelessWidget {
  final course;
  const SessionGrades({Key? key, required this.course}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
